START foldl START <- START ELT fn START start [ELT] tab
  @rec i = 0 start = start
    i == length tab ? start
    rec (i + 1) (fn start tab[i])

START foldr START <- ELT START fn START start [ELT] tab
  @rec i = length tab - 1 start = start
    i < 0 ? start
    rec (i - 1) (fn tab[i] start)
