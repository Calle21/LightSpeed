seqwrite ({E} seq, [E] sarr, Int start)
  if length of seq > (sarr.size - start)
    error "Sequence too large"
    else write (i = 0)
      if #s (i + seq.start) == seq.end
        (sarr, start + i)
        else
        set sarr.(start + i) to seq.#s >> write (i + 1)
