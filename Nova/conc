conc ([{E}] seqs, Int size) the {E}
  len = rec (i = 0, len = 0)
          if i == size
            len
            else rec (i + 1, len + length of seqs.i)
  build (newseq = seq len, i = 0, j = 0)
    if i == size
      newseq
      else (newseq, j) = seqwrite (seqs.i (newseq.array, len) j)
           build (newseq, i + 1, j)
