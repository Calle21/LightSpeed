synonym Ref A = Ref A

A read (Ref A a) = read a

Void (Ref A r) := (A a) = r := a

Void (Ref A r) += (A a) = r := read r + a

Void (Ref A r) -= (A a) = r := read r - a

Void (Ref A r) *= (A a) = r := read r * a

Void (Ref A r) /= (A a) = r := read r / a
