type Char = Unsigned 8

type Cons ELT = (ELT car, List ELT cdr)

type Int = Signed

type Int n = Signed (0,)

type Int neg = Signed (,-1)

type Int pos = Signed (1,)

type List ELT = Cons ELT | Void

type Map KEY ELT = (Map KEY ELT left, KEY key, ELT elt, Map KEY ELT left) | Void

type Set LEAF = (Set LEAF left, LEAF leaf, Set LEAF right) | Void

type Sequence ELT = (Array elt, Nat n, Nat n)

type Table ELT = (Array ELT, Nat n)

type Void = ()
void = ()
