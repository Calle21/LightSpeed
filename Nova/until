until (Bool <- E p) (S seq)
  if empty seq
    fail
    else
    if p #c (car seq)
      #c
      else
      until p (cdr seq)

until (Bool <- E p) ([E] tab) = rec (i = 0)
  if
   i == tab.size -> fail
   p #i tab@i    -> #i
    else
    rec (i + 1)
